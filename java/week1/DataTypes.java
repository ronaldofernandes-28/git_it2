public class DataTypes {
    public static void main(String[] args) {
        // Declare and initialize various data types
        // 0 , 1 1 bit
        //8 bits = 1 byte
        // Integer data types
        int rollNo;
        rollNo = 28;
        int a = 10; // 4 bytes = 32 bits
        short s = 15; // 2 bytes = 16 bits
        byte b = 11; // 1 byte = 8 bits
        long l = 117; // 8 bytes = 64 bits
        double d = 10.5; // 8 bytes = 64 bits precision difference between float and double, has more precision than float
        float f = 10.5f; // 4 bytes = 32 bits
        // Character data type
        char ch = 'a'; // 2 bytes = 16 bits unicode character use single quotes for char and double quotes for string
        // Boolean data type
        boolean bool = true; // 1 bit true or false 1 byte = 8 bits

        // Declare an integer variable and assign a value to it
        int b1;
        b1 = 10;
        // variables cant start with numbers
        // int Var, int var are two different variables
        // int var1 is valid but int 1var is not valid
        //int isPrime // camel case first letter small second word onwards first letter capital
        /* C:

A variable name can contain letters, digits, and underscores.
A variable name must start with a letter or an underscore.
Variable names are case-sensitive.
Variable names cannot contain whitespaces or special characters like !, #, %, etc.
Reserved words (like C keywords, such as int) cannot be used as names.
C++:

A variable name can contain letters, digits, and underscores.
A variable name must start with a letter or an underscore.
Variable names are case-sensitive.
Variable names cannot contain whitespaces or special characters like !, #, %, etc.
Reserved words (like C++ keywords, such as int) cannot be used as names.
Java:

A variable name can contain letters, digits, underscores, and dollar signs.
A variable name must start with a letter or an underscore.
Variable names are case-sensitive.
Variable names cannot contain whitespaces or special characters like !, #, %, etc.
Reserved words (like Java keywords, such as int) cannot be used as names.
Python:

A variable name can contain letters, digits, and underscores.
A variable name must start with a letter or an underscore character.
Variable names are case-sensitive (age, Age and AGE are three different variables).
A variable name cannot start with a number. */
    }
}
